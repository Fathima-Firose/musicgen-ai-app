services:
  - type: web
    name: musicgen-demo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --bind 0.0.0.0:$PORT --worker-class gthread --threads 2
